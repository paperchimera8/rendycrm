services:
  rendycrm:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      CRM_UPSTREAM: ${CRM_UPSTREAM:-http://127.0.0.1:3000}
    ports:
      - "${PORT:-8080}:8080"
    restart: unless-stopped
