services:
  rendycrm:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      CRM_UPSTREAM: ${CRM_UPSTREAM:-http://127.0.0.1:3000}
    ports:
      - "${HOST_PORT:-8081}:8080"
    restart: unless-stopped
