services:
  rendycrm:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      CRM_UPSTREAM: ${CRM_UPSTREAM:-https://paperchimera8-rendycrm-backend-ba64.twc1.net}
    ports:
      - "${HOST_PORT:-8081}:8080"
    restart: unless-stopped
